// AUTO-GENERATED by env.py. DO NOT EDIT.
#include "cuffpa/logging.cuh"
#include "ffpa_attn_bwd_decls.h"

void ffpa_attn_bwd_fp16f32(
    torch::Tensor Q,
    torch::Tensor K,
    torch::Tensor V,
    torch::Tensor O,
    torch::Tensor softmax_lse,
    torch::Tensor dO,
    torch::Tensor dQ,
    torch::Tensor dK,
    torch::Tensor dV,
    int stages,
    int causal,
    double softmax_scale) {
  CHECK_TORCH_TENSOR_DTYPE(Q, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(K, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(V, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(O, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dO, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dQ, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dK, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dV, torch::kHalf)
  const int d = Q.size(3);
  switch (d) {
    case 288: ffpa_attn_bwd_fp16f32_d288(Q, K, V, O, softmax_lse, dO, dQ, dK, dV, stages, causal, softmax_scale); break;
    default: throw std::runtime_error("bwd: headdim not supported!");
  }
}

void ffpa_attn_bwd_bf16f32(
    torch::Tensor Q,
    torch::Tensor K,
    torch::Tensor V,
    torch::Tensor O,
    torch::Tensor softmax_lse,
    torch::Tensor dO,
    torch::Tensor dQ,
    torch::Tensor dK,
    torch::Tensor dV,
    int stages,
    int causal,
    double softmax_scale) {
  CHECK_TORCH_TENSOR_DTYPE(Q, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(K, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(V, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(O, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dO, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dQ, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dK, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dV, torch::kBFloat16)
  const int d = Q.size(3);
  switch (d) {
    case 288: ffpa_attn_bwd_bf16f32_d288(Q, K, V, O, softmax_lse, dO, dQ, dK, dV, stages, causal, softmax_scale); break;
    default: throw std::runtime_error("bwd: headdim not supported!");
  }
}

void ffpa_attn_bwd_persistent_kv_fp16f32(
    torch::Tensor Q,
    torch::Tensor K,
    torch::Tensor V,
    torch::Tensor O,
    torch::Tensor softmax_lse,
    torch::Tensor dO,
    torch::Tensor dQ,
    torch::Tensor dK,
    torch::Tensor dV,
    int stages,
    int causal,
    double softmax_scale) {
  CHECK_TORCH_TENSOR_DTYPE(Q, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(K, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(V, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(O, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dO, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dQ, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dK, torch::kHalf)
  CHECK_TORCH_TENSOR_DTYPE(dV, torch::kHalf)
  const int d = Q.size(3);
  switch (d) {
    case 288: ffpa_attn_bwd_persistent_kv_fp16f32_d288(Q, K, V, O, softmax_lse, dO, dQ, dK, dV, stages, causal, softmax_scale); break;
    default: throw std::runtime_error("bwd: headdim not supported!");
  }
}

void ffpa_attn_bwd_persistent_kv_bf16f32(
    torch::Tensor Q,
    torch::Tensor K,
    torch::Tensor V,
    torch::Tensor O,
    torch::Tensor softmax_lse,
    torch::Tensor dO,
    torch::Tensor dQ,
    torch::Tensor dK,
    torch::Tensor dV,
    int stages,
    int causal,
    double softmax_scale) {
  CHECK_TORCH_TENSOR_DTYPE(Q, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(K, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(V, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(O, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dO, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dQ, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dK, torch::kBFloat16)
  CHECK_TORCH_TENSOR_DTYPE(dV, torch::kBFloat16)
  const int d = Q.size(3);
  switch (d) {
    case 288: ffpa_attn_bwd_persistent_kv_bf16f32_d288(Q, K, V, O, softmax_lse, dO, dQ, dK, dV, stages, causal, softmax_scale); break;
    default: throw std::runtime_error("bwd: headdim not supported!");
  }
}
